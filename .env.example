# Local Homebrew Postgres (current machine):
DATABASE_URL="postgresql://USER@localhost:5432/longhand?schema=public"
# Docker alternative: postgresql://longhand:longhand@localhost:5432/longhand?schema=public
SESSION_SECRET="replace-with-a-long-random-string"

SEED_USER_EMAIL="aspirant@localhost"
SEED_USER_PASSWORD="longhand"

# One line only — not the curl from the Anthropic docs.
ANTHROPIC_API_KEY="sk-ant-..."
